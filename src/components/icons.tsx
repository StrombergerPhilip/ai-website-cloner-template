export function BrandMark({ className }: { className?: string }) {
  return (
    <span
      className={className}
      style={{
        fontWeight: 500,
        letterSpacing: "-0.01em",
      }}
    >
      Shop
    </span>
  );
}
